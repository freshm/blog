<% if signed_in? %>
	<% if current_user.admin? %>
	   <br />
        <%= link_to "Add a new entry", new_blog_post_path, class: "icon-plus"%>
    	<br />
    	<br />
	<% end %>
    
    <%= will_paginate @blogposts %>
    <%= render partial: 'layouts/post', locals: { :posts => @blogposts } %>
    <%= will_paginate @blogposts %>
    
<% else %>
    <%= link_to "Sign up!", signup_path, class: "icon-signin"%>
<% end %>